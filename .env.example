# Local secrets — never commit. Copy from .env.example.
# Get a key (no billing required to try): https://aistudio.google.com/apikey
# Free-tier rate limits are low; see README “Configure your Gemini API key”.
# Paid billing unlocks higher quotas and Google Search grounding for missing
# venue/city/state.
GEMINI_API_KEY=
DAT_TRACKER_LLM_MODEL=gemini-3.6-flash
DAT_TRACKER_LLM_PRO_MODEL=gemini-3.1-pro-preview
